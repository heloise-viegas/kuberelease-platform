apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "commerce-api.configMapName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "commerce-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
